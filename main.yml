---
- name: copy the banner
  hosts: web
  become: yes
  tasks:
    - name: copy the benner content
      ansible.builtin.copy:
        content: "Sun set down in east"
        dest: /etc/motd.d/banner

